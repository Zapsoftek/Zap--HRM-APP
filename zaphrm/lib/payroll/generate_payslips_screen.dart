import 'package:flutter/material.dart';

class GeneratePayslipsScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('Generate Payslips')),
      body: Center(
        child: Text('Payslip generation functionality will appear here.'),
      ),
    );
  }
}
